-- +goose Up
-- Восстановлена семантика полей (без изменения схемы):
-- quantity — число упаковок (учётных коробок / бутылок и т.д.),
-- volume_per_unit — сколько единиц меры в одной упаковке (л, мл, кг, г или штук в коробке).
-- Итог в базовых единицах = quantity * COALESCE(volume_per_unit, 1).
-- Строки после 00016 с volume_per_unit IS NULL и measure_unit = milliliter по-прежнему совместимы:
-- клиент трактует отсутствие как 1 мл на «упаковку», итог совпадает с сохранённым quantity как суммарными мл.

SELECT 1;

-- +goose Down
SELECT 1;
